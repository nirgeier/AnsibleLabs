name: Lab-013.yaml

on:
  push:
    branches:
      - main
      - mkdocs
      - gh-pages
    paths:
      - "Labs/013-adhoc-commands/**"
  pull_request:
    branches:
      - main
      - mkdocs
      - gh-pages
    paths:
      - "Labs/013-adhoc-commands/**"

jobs:
  build:
    runs-on: ubuntu-latest
    # Execute the step demo to verify that all is working
    steps:
      # Check out the code
      - uses: actions/checkout@v5

      - name: Test Lab
        working-directory: Labs/013-adhoc-commands
        run: bash ./_demo.sh
